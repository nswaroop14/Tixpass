## Packages
qrcode.react | Generating QR codes for tickets
@yudiel/react-qr-scanner | Modern React 18 component for scanning QR codes
date-fns | Date formatting and manipulation

## Notes
Backend uses JWT for authentication, returned in /api/auth/login.
Frontend must store this token in localStorage and attach it to all protected API requests via Authorization header.
